# 更新记录

只记录 1.0 以后的正式版本。安装与更新方法见 README。

## 1.0.3（2026-09-04）

- 说明书补充轮询节奏：任务结果里的 `poll_after_seconds` 是下一次该等多久，不要问得更勤。
- 转写计价改为每开始的 5 分钟 1 积分（原来是 10 分钟），说明书里的说法同步更新。
- Codex 版的介绍改成和 Claude 版一致的口径：只在你提到存过的资料、问「一般怎么做」或点名自己关注的主题时才顺手找一次库，不主动搜。

## 1.0.2（2026-09-04）

- 按内容搜库支持翻页：结果被截断时带 `next_offset`，说明书写明每条资料最多 5 处命中、每次最多 50 条，agent 需要更多再翻。
- 桌面端 Claude 提供 `.plugin` 安装包，和命令行安装内容一致。

## 1.0.1（2026-09-03）

- 主动搜库改为「三条线索才搜、只搜一次」：用户提到存过或看过的资料，问「一般怎么做」并要来源，或点名自己关注的主题问库里有什么。命中只提一句，没命中不汇报，对话开头不自作主张搜。
- 触发描述精简，去掉「任何教程能帮到的任务都触发」一类过宽的说法。
- 不再依赖 hooks：只有部分客户端支持，用户还要另外装，去掉。

## 1.0.0（2026-09-03）

- 正式版：32 个 MCP 工具与一份说明书，覆盖收集、整理、字幕、评论、笔记、练习、导出与用量查询。
- 字幕默认干净整段，不再宣传「原话与时间点」；口径可按需切换。
- 最低兼容的站点版本为 1.0.0；更早的内测版本（0.x）不再提供。
